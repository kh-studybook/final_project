<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Reserves">

	<select id="getReivewList" parameterType="map"
		resultType="reviewinfo">
		select *
		from (select rownum rnum, reviewInfo.*
		from (select r.review_code, r.room_code, r.content, r.review_date,
		r.mem_key, m.name, m.profile
		from review r, member m
		where
		r.mem_key=m.key
		and r.room_code=#{room_code}
		order by r.review_date)
		reviewInfo )
		where rnum &gt;= #{start} and rnum &lt;= #{end}
	</select>

	<select id="getReviewCount" parameterType="int" resultType="int">
		select count(*) from review
		where room_code=#{room_code}
	</select>
	<!-- 나의 후기 검색관련 -->
	<select id="getSearchList" parameterType="map"
		resultType="review">
		select *
		from (select rownum rnum, b.*
		from (select re.r_code, ro.room_name, re.reserve_date, re.start_time,
		re.end_time,rv.review_code, rv.review_date, rv.content,go.file_name
		from room ro, reserve re, review rv, gallery go
		where ro.room_code=re.room_code and re.r_code=rv.r_code and go.room_code =re.room_code and go.gallery_num=1 and re.mem_key=#{key} order by rv.r_code desc ) b)
		where rnum &gt;= #{start} and rnum &lt;= #{end}
	</select>
	<select id="searchcount" parameterType="int" resultType="int">
		select count(*)
		from review where mem_key=#{key}
	</select>
</mapper>